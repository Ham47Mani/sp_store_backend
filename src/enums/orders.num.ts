// --- Payment Status Enum
export enum paymentStatus {
  pending = 'pending',
  paid = 'paid',
  failed = 'failed'
}

// --- Order Status Enum
export enum orderStatus {
  pending = 'pending',
  completed = 'completed'
}